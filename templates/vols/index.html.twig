{% extends 'base.html.twig' %}

{% block title %}Hello VolsController!{% endblock %}

{% block body %}

 <section class="form">
        <div class="container">
 <div class="row">
                <div class="col"></div>
                <div class="col-6">
    {{ form_start(form) }}


                    <div class="mb-3">
                    {{ form_label(form.depart, 'depart', {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_row(form.depart , {'attr' : {'class' : 'form-control'}}) }}
                    </div>

                    {{ form_label(form.destination, 'destination', {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_row(form.destination  , {'attr' : {'class' : 'form-control'}}) }}

                 



        {{ form_rest(form) }}
         </div>
                <div class="col"></div>
            </div>
        </div>
    </section>
    {{ form_end(form) }}


    
{% endblock %}









